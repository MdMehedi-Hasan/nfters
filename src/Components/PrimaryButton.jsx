export default function PrimaryButton({message}) {
  return (
      <button className='bg-primary hover:scale-105 transition-all ease-in-out text-white px-5 py-1 rounded-full'>{message}</button>
  )
}
